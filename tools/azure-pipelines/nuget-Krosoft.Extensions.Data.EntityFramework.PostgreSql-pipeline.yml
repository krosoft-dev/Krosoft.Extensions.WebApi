name: Nuget 'Krosoft.Extensions.Data.EntityFramework.PostgreSql' - $(Date:yyyy.MM.dd).$(BuildID)

pr: none

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - "**/Krosoft.Extensions.Data.EntityFramework.PostgreSql/*"

extends:
  template: ./templates/pipelines/nuget-template.yml
  parameters:
    packages:
     - "Krosoft.Extensions.Data.EntityFramework.PostgreSql"
     - "Krosoft.Extensions.Data.EntityFramework"

